<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil de Usuário</title>
    <link rel="stylesheet" href="/public/style.css">
</head>
<body>

    <div class="container">
        <div class="left">
            <!-- Exibe a imagem de perfil do usuário -->
            <img src="/uploads/<%= usuario.imagemPerfil %>" alt="Imagem de Perfil" class="profile-img">
            <form action="/upload" method="POST" enctype="multipart/form-data">
                <input type="file" name="imagemPerfil" accept="image/*" required>
                <button type="submit">Alterar Imagem</button>
            </form>
        </div>

        <div class="right">
            <h1>Perfil de <%= usuario.nome %></h1>
            <p><strong>Email:</strong> <%= usuario.email %></p>
            <p><strong>Imagem de Perfil:</strong> <img src="/uploads/<%= usuario.imagemPerfil %>" alt="Imagem de Perfil Atual" class="current-img"></p>
        </div>
    </div>

</body>
</html>
